#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// Template pour migration des secrets hors de main.cpp.
// Copier en credentials.h et remplir. Ne jamais versionner credentials.h.

struct WifiCredential {
  const char* ssid;
  const char* password;
};

// Liste des réseaux WiFi (scan, tri par RSSI, tentative avec BSSID/canal)
static const WifiCredential WIFI_LIST[] = {
  {"VOTRE_SSID_1", "VOTRE_MOT_DE_PASSE_1"},
  {"VOTRE_SSID_2", "VOTRE_MOT_DE_PASSE_2"},
  {"VOTRE_SSID_3", "VOTRE_MOT_DE_PASSE_3"}
};
#define WIFI_COUNT (sizeof(WIFI_LIST) / sizeof(WIFI_LIST[0]))

const char* SERVER_NAME = "iot.olution.info";
const char* SERVER_PATH = "/msp1/msp1gallery/upload.php";

#endif
