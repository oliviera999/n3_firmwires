#pragma once

// Header de compatibilité (DÉPRÉCIÉ).
// Les constantes canonique (BufferConfig/DisplayConfig/etc.) sont centralisées dans `include/config.h`.
// Objectif: éviter la divergence des valeurs si ce fichier est inclus par erreur.

#include "config.h"
