#pragma once

// Header de compatibilité (DÉPRÉCIÉ).
// La configuration canonique est centralisée dans `include/config.h`.
// Objectif: éviter la divergence des valeurs si ce fichier est inclus par erreur.

#include "config.h"
