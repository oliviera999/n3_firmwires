#pragma once

// Header de compatibilité (DÉPRÉCIÉ).
// La configuration canonique (ProjectConfig/Utils) est centralisée dans `include/config.h`.
// Objectif: éviter d'exposer une version différente si ce fichier est inclus par erreur.

#include "config.h"
