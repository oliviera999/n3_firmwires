#pragma once
// FFP5CS - Configuration réseau / serveur / mailer (façade v13.60).
// Voir `config.h` pour le contenu (NetworkConfig, ServerConfig, ApiConfig,
// WebAuthConfig, EmailConfig). Le découpage physique sera réalisé plus tard.
#include "config.h"