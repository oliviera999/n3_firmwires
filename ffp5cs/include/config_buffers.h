#pragma once
// FFP5CS - Configuration buffers / NVS bounds / heap thresholds (façade v13.60).
// Voir `config.h` pour le contenu effectif (BufferConfig, NVSConfig, HeapConfig,
// AsyncTaskConfig). Le découpage physique sera réalisé dans une release dédiée.
#include "config.h"