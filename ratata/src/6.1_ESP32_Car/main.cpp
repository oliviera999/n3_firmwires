/**
 * Ratata — 6.1_ESP32_Car (Kit ZYC0108-EN)
 * Voiture avec caméra WiFi ; stream HTTP/WebSocket possible.
 * Carte : ESP32-CAM.
 * Placeholder : à remplacer par le code du kit ou par ANM-P4F/ESP32-CAM-ROBOT.
 */

#include <Arduino.h>

void setup() {
    Serial.begin(115200);
    Serial.println(F("[6.1_ESP32_Car] Ratata ZYC0108-EN — placeholder"));
}

void loop() {
    // TODO : serveur HTTP + WebSocket stream, commandes (cf. ANM-P4F)
    delay(100);
}
