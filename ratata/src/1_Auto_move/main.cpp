/**
 * Ratata — 1_Auto_move (Kit ZYC0108-EN)
 * Déplacement de base : avancer, reculer, tourner.
 * Carte : Arduino UNO. Broches moteurs PWM 5/6, 74HCT595N.
 * Placeholder : à remplacer par le code du kit ou par le projet ANM-P4F (UNO moteurs).
 */

#include <Arduino.h>

void setup() {
    Serial.begin(9600);
    Serial.println(F("[1_Auto_move] Ratata ZYC0108-EN — placeholder"));
}

void loop() {
    // TODO : logique déplacement (avancer / reculer / tourner)
    delay(1000);
}
